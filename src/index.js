import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';


ReactDOM.render(
  <App />,  // Renderable component
  document.getElementById('root') // The block to index.html in which we insert the renderable component
);
